# =============================================================================
# Bitcoin Stack Configuration
# =============================================================================
# Copy this file to .env and customize for your setup.
# Run ./scripts/setup.sh (Linux/Mac) or ./scripts/setup.ps1 (Windows) to get started.
# =============================================================================

# -----------------------------------------------------------------------------
# PATHS
# -----------------------------------------------------------------------------

# Main data directory (blockchain, LND wallet, Tor data)
# Use absolute path for production, relative for development
DATA_DIR=./data

# Optional: Store blockchain on a separate drive (leave empty to use DATA_DIR)
# Useful when you have limited SSD space but large HDD for blockchain
# Example: /mnt/hdd/bitcoin-blocks or D:/bitcoin-blocks
BITCOIN_BLOCKS_DIR=

# -----------------------------------------------------------------------------
# BITCOIN KNOTS
# -----------------------------------------------------------------------------

# Bitcoin Knots version (check https://bitcoinknots.org for latest)
BITCOIN_VERSION=29.1.knots20250903

# Network: mainnet, testnet, signet, regtest
BITCOIN_NETWORK=mainnet

# RPC credentials (CHANGE THE PASSWORD!)
# Generate with: openssl rand -hex 32
BITCOIN_RPC_USER=bitcoinrpc
BITCOIN_RPC_PASS=CHANGE_ME_GENERATE_WITH_SETUP_SCRIPT

# Ports (change if running multiple nodes or conflicts)
BITCOIN_RPC_PORT=8332
BITCOIN_P2P_PORT=8333

# Pruning: 0 = full node (600GB+), or specify MB to keep (e.g., 50000 for 50GB)
BITCOIN_PRUNE=0

# Transaction index: 1 = enable (required for some wallets), 0 = disable
# Adds ~30GB to storage but enables txindex lookups
BITCOIN_TXINDEX=1

# Database cache in MB (more = faster sync, uses more RAM)
# Recommended: 4096 for 8GB+ RAM, 2048 for 4-8GB RAM, 1024 for <4GB RAM
BITCOIN_DBCACHE=4096

# Maximum peer connections (default: 40)
BITCOIN_MAXCONNECTIONS=40

# Memory limits for Docker
BITCOIN_MEM_LIMIT=8g
BITCOIN_MEM_RESERVATION=4g

# -----------------------------------------------------------------------------
# LND (Lightning Network Daemon)
# -----------------------------------------------------------------------------

# LND version (check https://github.com/lightningnetwork/lnd/releases)
LND_VERSION=v0.18.3-beta

# Your node's display name on the Lightning Network
LND_ALIAS=MyLightningNode

# Your node's color (hex format) - shows in network explorers
LND_COLOR=#FF6600

# Ports
LND_GRPC_PORT=10009
LND_REST_PORT=8080
LND_P2P_PORT=9735

# Log level: trace, debug, info, warn, error, critical
LND_DEBUG_LEVEL=info

# Memory limits for Docker
LND_MEM_LIMIT=2g
LND_MEM_RESERVATION=512m

# -----------------------------------------------------------------------------
# DOCKER
# -----------------------------------------------------------------------------

# Project name (used for container naming)
COMPOSE_PROJECT_NAME=bitcoin-stack

# User/Group ID for file permissions (match your host user)
# Find with: id -u (Linux/Mac) or use 1000 (default)
USER_ID=1000
GROUP_ID=1000
